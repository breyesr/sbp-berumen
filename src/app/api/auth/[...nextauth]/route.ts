// This file is required for Auth.js to handle authentication requests.
// It should use the full Node.js-compatible configuration.
import { handlers } from "@/lib/auth";
export const { GET, POST } = handlers;
